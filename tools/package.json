{
  "name": "tennis-planner-tools",
  "private": true,
  "type": "module",
  "description": "Match-data scrapers — dev only, never deployed. Output is vercel-deploy/data/matches.json.",
  "scripts": {
    "scrape:jttl": "node scrape-jttl.mjs",
    "test": "node --test \"test/*.test.mjs\""
  }
}
