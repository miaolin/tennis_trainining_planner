{
  "name": "tennis-planner-tests",
  "private": true,
  "type": "module",
  "description": "jsdom harness for vercel-deploy/index.html — dev only, never deployed",
  "scripts": {
    "test": "node planner.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
